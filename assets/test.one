int a = 1;
float b = 3.14;
string c = "teste";
bool d = true;

int multiplica(int a, int b) {
    return a * b;
}

float divide(float a, int b) {
    return a / b;
}

int main() {
    a = a + 2 * 1 + 3;
    b = b - 2;

    if (a <  b) {
        a = a + 1;
    } else if (a > b) {
        a = a - 1;
    } else {
        a = b;
    }

    int x = 0;

    while(x < 10) {
        x = x + 1;
    }

    for (int y = 0; y < 10; y = y + 1) {
        x = x - 1;
    }

    return 0;
}

